'use client'

import { useState } from 'react'
import { Search, Users } from 'lucide-react'
import { students } from '@/lib/staff-data'
import { PageHeader, PrivacyBadge } from '@/components/staff/staff-ui'

export default function StudentsPage() { const [query, setQuery] = useState(''); const [filter, setFilter] = useState('all'); const shown = students.filter((s) => (filter === 'all' || s.status === filter) && `${s.id} ${s.name}`.toLowerCase().includes(query.toLowerCase())); return <><PageHeader eyebrow="Students" title="A high-level view." description="Only permitted identifiers and broad trend labels are shown here. Student reflections remain private." action={<PrivacyBadge />} /><div className="mb-5 flex flex-col gap-3 sm:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-3 size-4 text-muted" /><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search student ID" className="h-10 w-full rounded-xl border border-line bg-white pl-9 pr-3 text-sm font-semibold outline-none focus:border-ink" /></label><select value={filter} onChange={(e) => setFilter(e.target.value)} className="h-10 rounded-xl border border-line bg-white px-3 text-sm font-bold text-ink"><option value="all">All students</option><option value="Steady">Steady</option><option value="Watch">Watch</option><option value="Support recommended">Support recommended</option></select></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{shown.map((student) => <article key={student.id} className="rounded-2xl border border-line bg-white p-5 shadow-card"><div className="flex items-start justify-between"><div className="flex gap-3"><div className="flex size-10 items-center justify-center rounded-xl bg-ice"><Users className="size-4 text-ink" /></div><div><h2 className="text-sm font-extrabold text-ink">{student.id}</h2><p className="mt-1 text-xs font-semibold text-muted">{student.year} · {student.lastCheckIn}</p></div></div><span className={`rounded-full px-2.5 py-1 text-[11px] font-extrabold ${student.status === 'Steady' ? 'bg-[#ddf5e7] text-[#277348]' : student.status === 'Watch' ? 'bg-[#fff4bf] text-ink' : 'bg-[#ffe3e8] text-[#9e344a]'}`}>{student.status}</span></div><div className="mt-5 flex items-center justify-between border-t border-line pt-4 text-xs font-bold text-muted"><span>{student.checkIns} check-ins</span><span>Trend: {student.trend}</span></div></article>)}</div></> }
