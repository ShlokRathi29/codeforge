import Link from 'next/link'
import { notFound } from 'next/navigation'
import { AlertTriangle, ArrowLeft, Check, LockKeyhole, MessageCircleHeart } from 'lucide-react'
import { getAlert } from '@/lib/staff-data'
import { AlertBadge, PageHeader, PrivacyBadge, TrendChart } from '@/components/staff/staff-ui'

export default async function StudentAlertPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const alert = getAlert(id); if (!alert) notFound(); return <><Link href="/staff/alerts" className="mb-6 inline-flex items-center gap-2 text-sm font-extrabold text-muted hover:text-ink"><ArrowLeft className="size-4" />Back to alerts</Link><PageHeader eyebrow="Alert detail" title={alert.kind} description={`${alert.studentName} · Generated ${alert.createdAt}`} action={<PrivacyBadge />} /><div className="grid gap-5 lg:grid-cols-[1.15fr_.85fr]"><section className="rounded-2xl border border-line bg-white p-6 shadow-card sm:p-7"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="eyebrow">{alert.id}</p><h2 className="mt-2 font-display text-2xl font-extrabold tracking-[-.05em] text-ink">A pattern to consider</h2></div><AlertBadge status={alert.status} /></div><p className="mt-5 text-sm leading-6 text-muted">{alert.reason}</p><ul className="mt-6 flex flex-col gap-3">{alert.bullets.map((bullet) => <li key={bullet} className="flex gap-3 text-sm font-semibold text-ink"><Check className="mt-0.5 size-4 shrink-0 text-[#277348]" />{bullet}</li>)}</ul><div className="mt-7 border-t border-line pt-6"><p className="eyebrow">Recommended human action</p><div className="mt-3 flex gap-3 rounded-xl bg-ice p-4"><MessageCircleHeart className="mt-0.5 size-5 shrink-0 text-ink" /><p className="text-sm leading-6 text-muted">Offer a low-pressure check-in and ask what kind of support would feel useful. Do not lead with the alert or assume what the student needs.</p></div></div></section><section className="rounded-2xl border border-line bg-white p-6 shadow-card sm:p-7"><p className="eyebrow">Recent trend</p><h2 className="mt-2 font-display text-xl font-extrabold tracking-[-.04em] text-ink">Wellbeing and stress</h2><div className="mt-5"><TrendChart data={alert.trend} dataKey="wellbeing" /></div><div className="mt-4 flex items-start gap-3 rounded-xl bg-[#f7fafc] p-4"><LockKeyhole className="mt-0.5 size-4 shrink-0 text-muted" /><p className="text-xs leading-5 text-muted">This view contains aggregate check-in signals only. Private notes, journal entries, and individual responses are intentionally excluded.</p></div><div className="mt-5 flex flex-wrap gap-2"><button className="rounded-full bg-yellow px-4 py-2.5 text-xs font-extrabold text-ink"><Check className="mr-1 inline size-3" />Mark reviewed</button><button className="rounded-full border border-line px-4 py-2.5 text-xs font-extrabold text-ink">Needs follow-up</button></div></section></div></> }
